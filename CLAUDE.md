# Instructions

- NEVER use the em dash character (—) anywhere. Not in chat responses, not in code, not in comments, not in commit messages. Use a period, comma, or "and" instead.
- NEVER add a `Co-Authored-By: Claude...` line or a `Claude-Session:` link to a commit message. Write commits as if Ahmad wrote them himself.
